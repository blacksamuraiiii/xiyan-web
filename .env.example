# 析言数据分析助手环境配置示例文件
# XiYan Data Analysis Assistant Environment Configuration Example

# =============================================================================
# 视觉语言模型配置 (用于OCR功能)
# Visual Language Model Configuration (for OCR functionality)
# =============================================================================
VL_MODEL_BASEURL=YOUR_VL_MODEL_API_BASE_URL
VL_MODEL_KEY=YOUR_VL_MODEL_API_KEY
VL_MODEL_NAME=qwen-vl-plus

# 推荐模型: Qwen2.5-VL-7B-Instruct
# https://huggingface.co/Qwen/Qwen2.5-VL-7B-Instruct

# =============================================================================
# SQL生成模型配置 (用于自然语言转SQL)
# Text-to-SQL Model Configuration (for natural language to SQL conversion)
# =============================================================================
SQL_MODEL_BASEURL=YOUR_SQL_MODEL_API_BASE_URL
SQL_MODEL_KEY=YOUR_SQL_MODEL_API_KEY
SQL_MODEL_NAME=sql-agent

# 推荐模型: XiYanSQL-QwenCoder-32B-2412
# https://huggingface.co/XGenerationLab/XiYanSQL-QwenCoder-32B-2412

# =============================================================================
# PostgreSQL数据库连接配置
# PostgreSQL Database Connection Configuration
# =============================================================================
# 注意: 如果使用Docker Compose部署，这些配置会被docker-compose.yaml中的环境变量覆盖
# Note: When using Docker Compose, these configurations will be overridden by 
# environment variables in docker-compose.yaml

DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_DATABASE=your_db_name

# =============================================================================
# 应用配置 (可选)
# Application Configuration (Optional)
# =============================================================================
# 日志级别 (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# 最大文件上传大小 (MB)
# MAX_FILE_SIZE=50

# OCR处理最大页数 (PDF文件)
# MAX_OCR_PAGES=3

# 数据库连接池大小
# DB_POOL_SIZE=5